import AuthForm from "../components/AuthForm";

const Auth = () => {
   return (
      <div className="flex-1 p-4 w-full flex justify-center items-center">
         <AuthForm />
      </div>
   );
};

export default Auth;
